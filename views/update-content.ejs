<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit User</title>
</head>
<body>
  <h1>Edit User</h1>
  <form id="updateForm">
    <input type="hidden" name="id" value="<%= data.id %>">
    <label>Name: <input type="text" name="name" value="<%= data.name %>" required></label><br><br>
    <label>Email: <input type="email" name="email" value="<%= data.email %>" required></label><br><br>
    <label>Phone: <input type="text" name="phone" value="<%= data.phone %>" required></label><br><br>
    <button type="submit">Update</button>
    <button type="button" onclick="window.location.href='/api/user/get-all-user'">Cancel</button>
  </form>

<script>
  document.getElementById('updateForm').addEventListener('submit', e => {
    e.preventDefault();

    const formData = new FormData(e.target);
    const id = formData.get('id');
    const data = {};
    formData.forEach((val, key) => { if(key !== 'id') data[key] = val });

    fetch(`/api/user/update-user/${id}`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data)
    })
    .then(res => res.json())
    .then(result => {
      alert('User updated!');
      window.location.href = '/api/user/get-all-user';
    })
    .catch(() => alert('Error updating user'));
  });
</script>
</body>
</html> -->
