<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>User List</title>
  <style>
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #ccc; padding: 10px; text-align: left; }
    th { background-color: #f2f2f2; }
    .button {
      display: inline-block;
      padding: 5px 10px;
      background-color: dodgerblue;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      margin-right: 5px;
      cursor: pointer;
      border: none;
    }
    .add-button {
      background-color: green;
      margin-bottom: 15px;
    }
  </style>
</head>
<body>
  <h1>User List</h1>

  Add New User button
  <button class="button add-button" onclick="window.location.href='/api/user/create-user'">Add New Record</button>

  <table>
    <thead>
      <tr>
        <th>ID</th><th>Name</th><th>Email</th><th>Phone</th><th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% if (data.length > 0) { %>
        <% data.forEach(user => { %>
          <tr>
            <td><%= user.id %></td>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
            <td><%= user.phone %></td>
            <td>
              <button class="button" onclick="window.location.href='/api/user/get-user/<%= user.id %>'">View</button>
              <button class="button" onclick="window.location.href='/api/user/update-user/<%= user.id %>'">Edit</button>
              <button class="button" style="background-color: red;" onclick="if(confirm('Are you sure?')) deleteUser('<%= user.id %>')">Delete</button>
            </td>
          </tr>
        <% }) %>
      <% } else { %>
        <tr><td colspan="5">No users found.</td></tr>
      <% } %>
    </tbody>
  </table>

<script>
  function deleteUser(id) {
    fetch(`/api/user/delete-user/${id}`, {
      method: 'DELETE'
    })
    .then(res => {
      if(res.ok) {
        alert('User deleted successfully');
        location.reload(); // refresh page
      } else {
        alert('Failed to delete user');
      }
    })
    .catch(() => alert('Error deleting user'));
  }
</script>

</body>
</html>  -->
