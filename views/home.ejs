<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Student List</title>
    <style>
        table { 
            border-collapse: collapse; width: 100%; 
        }
        th, td {
             border: 1px solid #ccc; padding: 10px; text-align: left;
        }
        th { 
            background-color: #f2f2f2; 
        }
        a.button {
            display: inline-block;
            padding: 5px 10px;
            background-color: dodgerblue;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin-right: 5px;
        }
        .add-button {
            margin-bottom: 10px;
            display: inline-block;
            background-color: green;
        }
    </style>
</head>
<body>
    <h1>Student Records</h1>

    <a class="button add-button" href="/add-content">Add New Record</a>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Age</th>
                <th>Email</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% if (data.length > 0) { %>
                <% data.forEach(student => { %>
                    <tr>
                        <td><%= student.id %></td>
                        <td><%= student.name %></td>
                        <td><%= student.age %></td>
                        <td><%= student.email %></td>
                        <td>
                            <a class="button" href="/single-student?id=<%= student.id %>">View</a>
                            <a class="button" href="/update-content?id=<%= student.id %>">Edit</a>
                            <a class="button" href="/delete-content?id=<%= student.id %>" onclick="return confirm('Are you sure you want to delete this record?');">Delete</a>
                        </td>
                    </tr>
                <% }); %>
            <% } else { %>
                <tr>
                    <td colspan="5">No records found.</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html> -->
